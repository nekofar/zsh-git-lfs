#
# Aliases
#

alias glfv="git lfs version"

alias glfi="git lfs install"
alias glfui="git lfs uninstall"

alias glfup="git lfs update"
alias glfupf="git lfs update--force"

alias glflk="git lfs lock"
alias glflks="git lfs locks"
alias glfulk="git lfs unlock"

alias glff="git lfs fetch"
alias glffa="git lfs fetch --all"
alias glffr="git lfs fetch --recent"
alias glffo="git lfs fetch origin"

alias glfl="git lfs pull"
alias glfp="git lfs push"
alias glfco="git lfs checkout"
alias glfst="git lfs status"

alias glflg="git lfs logs"
alias glflgc="git lfs logs clear"
alias glflgb="git lfs logs boomtown"

alias glfev="git lfs env"

alias glfls="git lfs ls-files"
alias glflsa="git lfs ls-files --all"
alias glflsl="git lfs ls-files --long"
alias glflsn="git lfs ls-files --name-only"

alias glfx="git lfs ext"
alias glfxl="git lfs ext list"

alias glfpo="git lfs pointer"
alias glfpof="git lfs pointer --file"
alias glfpocf="git lfs pointer --check --file"

alias glfsm="git lfs smudge"
alias glfsms="git lfs smudge --skip"

alias glfcl="git lfs clean"

alias glffp="git lfs filter-process"
alias glffps="git lfs filter-process --skip"

alias glffs="git lfs fsck"
alias glfdd="git lfs dedup"

alias glfpr="git lfs prune"
alias glfprd="git lfs prune --dry-run"
alias glfprv="git lfs prune --verbose"

alias glfstf="git lfs standalone-file"

alias glfm="git lfs migrate"
alias glfma="git lfs migrate info"
alias glfmi="git lfs migrate import"
alias glfmii="git lfs migrate import --include"
alias glfme="git lfs migrate export"
alias glfmei="git lfs migrate export --include"

alias glfpco="git lfs post-checkout"
alias glfpc="git lfs post-commit"
alias glfpm="git lfs post-merge"
alias glfpp="git lfs pre-push"

alias glft="git lfs track"
alias glftl="git lfs track --lockable"
alias glftf="git lfs track --filename"
alias glfut="git lfs untrack"

